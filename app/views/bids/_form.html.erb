<%= form_for @bid do |f|  %>
<% if @bid.errors.any? %>
    <div id='error_explanation'>
      <ul>
        <% @bid.errors.full_message.each do |msg| %>
          <ul><%= msg %> </ul>
        <% end%>
      </ul>
    </div>
  <% end %>

  <p>Bid: <%= f.text_field :bid%></p>
  <%= f.submit "Place Bid", class: 'btn btn-large btn-primary' %>
<% end %>
