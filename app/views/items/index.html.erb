<h1 class="center">My Items for Sale</h1>
<% @items.each do |item| %>
  <p><%= image_tag item.picture.to_s %></p>
  <p>Name: <%= item.name %></p>
  <% if item.bid.highest_bid == 0.0 %>
    <p>Starting Bid: <%= number_to_currency(item.start_bid) %></p>
  <% end %>
<% end %>
